# 진행 상황

> 작업을 마칠 때마다 이 파일을 갱신한다. 새 세션은 여기서부터 이어간다.

## 현재 상태 (2026-05-21)

- ✅ 기술 스택 확정: 순수 HTML/CSS/JS + GSAP
- ✅ 발표 형식 확정: 자유 인터랙티브, 슬라이드 단위 구성
- ✅ 프로젝트 문서 골격 생성 (AGENTS.md, docs/)
- ✅ **슬라이드 1 (인트로) 구현·확정 완료** — 검정 배경 + Fraunces + 모노톤 + 윤곽선/솔리드 + 팀원 4명
- ✅ 발표 주제 확정: **에너지 절약** / 프로젝트: **에너지 사용량 모니터링·관리 앱**
- ✅ **슬라이드 2 (핵심 인사이트 "관리") 구현·확정 완료** — 키워드 점등 → 전등 꺼지듯 소등 → "관리"만 개화
- ✅ **슬라이드 3 (측정 — 피터 드러커 인용) 구현 완료** — 좌우 분할(흑백 인물 사진 | 인용구), 사진 현상 → 측정·관리·개선 체인 점화 → "측정" 킥커
- ✅ 덱 내비게이션 추가: 세로 scroll-snap + 키보드(↑/↓/Space/PageUp·Down), R = 현재 슬라이드 재생
- ✅ 오프라인 동작 유지: GSAP·Fraunces·Pretendard 모두 로컬 보관, 새 의존성 없음
- ✅ 검증 완료: 콘솔 에러 0건, 슬라이드 2 타임라인 3개 프레임(풀스캐터·응축·최종) 컴퓨티드 스타일 확인, 오버플로 없음
- ✅ 슬라이드 3 검증: 콘솔 에러 0건, 타임라인 5단계 프레임(현상·체인 점화 중·점화 완료·종료) 컴퓨티드 스타일 확인.
  좌우 분할 비율(사진 42% 풀하이트 · 인용구 수직 중앙) 측정 확인, 콘텐츠 오버플로 없음.
  사진 `peter.png` 정상 로드(2046×1618) 확인. 인용구 두 문장 각각 한 줄에 들어감(폭 측정, 여유 ~48px).
- ✅ **슬라이드 4 (솔루션 — B-EMS→C-EMS) 디자인 전면 강화 완료** — 정사영 아이소메트릭 + 관제 대시보드.
  1막: 업라이트 빌딩(점등 창문·옥상 유닛·에너지 펄스) + 좌우 관제 콘솔(카운트업 수치·스파크라인·층별
  부하 막대·효율 게이지) + 발밑 글로우·격자 바닥.
  2막: 부감 줌아웃 → 캠퍼스 26채 + 중심 코어 네트워크 연결선 + 집계 스트립 → `C-EMS` 각인.
  회전을 정적 CSS `.tilt` 래퍼로 분리(GSAP은 스케일·이동만) → 빌딩이 절대 안 기울어짐. 새 의존성 없음.
- ✅ 슬라이드 4 검증: 콘솔 에러 0건. 빌딩 4개 수직 모서리 화면 수직 확인(`dx=0`). 1·2막 패널 오버플로 0,
  가로/세로 화면 모두 확인. 외부 URL·CDN·ES 모듈 참조 없음.
- ✅ **슬라이드 5 (예측 엔진 — ML 작동 원리) 구현 완료** — 가로 3단 수렴 파이프라인.
  입력 변수 8개(기상 3 + 캠퍼스 특성 5)가 윤곽선 칩으로 점등 → 연결선을 타고 빛 점이 모델로
  흘러들며 칩별 **가중치(`×0.86`…)가 적용** → 중앙 **예측 모델 패널의 가중치 행렬(6×4)이
  churn→lock-in** + `모델 학습` 막대 0→100% → 모델 완성에 맞춰 우측 **예측 전력 사용량이
  `142.8 kW`로 카운트업** + 슬림 예측 곡선 → 킥커 `맥락을 읽는 예측`. 자동 재생 타임라인(~9s).
  데모 화면 자체는 실제 앱 담당.
- ✅ 슬라이드 5 검증: 콘솔 에러 0건. GSAP 타임라인 `seek(t,false)`(suppressEvents 해제)로
  프레임 확인 — 가중치 적용 8/8, 행렬 churn 24/24·lock-in 24/24, 학습 0→100%, 카운트업 142.8.
  오버플로 0(1280×720), 3단 영역 겹침 0. 새 의존성 없음(GSAP 코어 AttrPlugin만).
- ✅ **슬라이드 7 (SWOT 분석) 구현 완료** (구 슬라이드 6 — 재번호) — 2×2 매트릭스. 참고 이미지를 모노톤으로 번안.
  ▸ **형태 대비:** 참고 이미지의 파란색 대신 솔리드↔윤곽선으로 사분면 구분 — 강점·기회(긍정)는
    솔리드 배지·고스트 글자·채운 마커, 약점·위협(부정)은 윤곽선 배지·text-stroke 고스트·빈 마커.
  ▸ **연출:** eyebrow → 외곽 프레임 → 십자 분할선 드로잉 → 중심 노드 → 네 사분면이 읽기 순서
    (S→W→O→T)로 캐스케이드(고스트·배지·라벨·항목 3개) → 킥커 `분석을 넘어 전략으로`.
  ▸ **인터랙티브:** 재생 종료 후 사분면 호버 포커스 — 한 칸을 떠올리고(scale·글로우) 나머지는
    흐려진다. 발표자가 사분면을 하나씩 짚으며 설명 가능.
  ▸ SWOT 문구는 `js/slide-7.js`의 `QUADS` 배열에서 한곳 편집. 순수 CSS Grid 레이아웃이라
    리사이즈 자동 대응. 새 의존성 없음.
- ✅ 슬라이드 7 검증: 콘솔 에러 0건. 타임라인 종료 프레임 컴퓨티드 스타일 확인 — eyebrow·십자선·
  사분면 4·고스트 4·항목 12·킥커 모두 opacity 1. 매트릭스 오버플로 0(1280×720, 고스트 글자는
  의도된 모서리 블리드 — `overflow:hidden` 클립), 호버 포커스(포커스 1.0 / 나머지 0.26)·복원 동작 확인.
- ✅ **슬라이드 6 (현장 적용 — Before → After 전환) 구현 완료** — 슬라이드 5 머신러닝 뒤 신규 삽입.
  ▸ **서사:** 학교 시설팀 직접 인터뷰로 검증한 활용 효과. "현재 방식(윤곽선) → 화살표 → Campus-EMS
    적용(솔리드 점화)" 전환 행 2개(공용 전자기기 운영 · 피크 전력 관리) + 향후 방향 밴드(학생 공개
    → 선의의 경쟁 → 능동적 절감). 인터뷰 출처 앵커가 상단 신뢰 도장. 킥커 `예측을 넘어 운영으로`.
  ▸ **형태 대비:** 현재는 윤곽선 카드, Campus-EMS는 솔리드 카드(코너 브래킷 + `--emsglow` 점화).
  ▸ **재번호:** 기존 SWOT 슬라이드 6 → 7 (`slide-6.*`→`slide-7.*`, `s6-`→`s7-`, `AiceSlide6`→`AiceSlide7`).
  ▸ 전환 행·향후 단계는 `js/slide-6.js`의 `ROWS`·`FUTURE` 배열에서 한곳 편집. 새 의존성 없음.

## 다음 단계

> **▶ 다음 세션 첫 행동:** 사용자에게 **슬라이드 6(현장 적용) 시각 확인** 피드백을 받고,
> 마무리 슬라이드를 논의한다. **슬라이드 1~7은 확정 흐름이니 요청 없이 건드리지 말 것.**
> 슬라이드 5·6·7은 사용자 시각 확인이 아직 남았다 — 피드백 시 반영.

1. [ ] 슬라이드 5·6·7 사용자 시각 확인 — 피드백 있으면 반영
2. [ ] 슬라이드 8(마무리) 구성 논의 — 발표 클로징 흐름 결정
3. [ ] 슬라이드 8 제작 — `AGENTS.md`의 "디자인 언어" + "슬라이드 추가 규칙" 준수
4. [ ] (선택) 슬라이드 간 전환 애니메이션 고도화 — 현재는 scroll-snap 기본 전환

## 작업 방식 (사용자와 합의된 흐름)

- **슬라이드를 한 장씩** 사용자와 논의하며 만든다. 한 번에 전부 만들지 않는다.
- 각 슬라이드: 디자인 논의 → 사용자 확인 → 코드 구현 → `docs/` 갱신.
- 시각 피드백이 필요하면 미리보기 서버 사용: `python -m http.server` 또는 `.claude/launch.json`의 `aice`.

## 비고

- 본문/UI 폰트: 당초 Inter+Noto Sans KR → **Pretendard** 단일 폰트로 정리 (한글+영문 커버, 단일 파일).
- 미리보기(백그라운드 탭)에서는 rAF 정지로 자동재생이 안 됨 — 실제 브라우저(포그라운드)에서는 정상.
  슬라이드 2 검증은 GSAP 타임라인을 `seek()`로 프레임 이동시켜 컴퓨티드 스타일로 확인했다.
- 거부된 디자인 방향(파란색·메탈릭 질감·bounce 이징)은 `AGENTS.md`의 "하지 말 것" 참고.
- 슬라이드 2 키워드 목록은 `js/slide-2.js`의 `KEYWORDS` 배열에서 바로 편집 가능.
- 슬라이드 4 캠퍼스는 매 재생마다 무작위 배치 — 건물 수·격자는 `js/slide-4.js`의 `buildCity`,
  카메라 각도·스케일·타이밍은 같은 파일 상단 상수(`ACT1/ACT2_*`)와 `build` 타임라인에서 조정.

## 미해결 질문

- 발표 시간 / 전체 슬라이드 수 미정
- 슬라이드 8 이후 구성 미정 (마무리 슬라이드 흐름·개수)
- 슬라이드 5·6·7 사용자 시각 확인 미완 (코드·검증은 완료)

## 변경 이력

- 2026-05-20: 프로젝트 시작. 기술 스택·발표 형식 확정, 문서 골격 생성, 슬라이드 1 인트로 시안 제작.
- 2026-05-20: 슬라이드 1 시안 반복(v1→v4). 검정 배경 + Fraunces 정자체 + 모노톤 확정.
- 2026-05-20: 슬라이드 1 시안 v6. 강조 방식 "윤곽선+솔리드" 확정, 팀원 4명 추가, i 진입 타이밍 수정.
- 2026-05-20: 슬라이드 1 확정 및 실제 코드 구현 완료. 오프라인 에셋 로컬화, 렌더링 검증 통과.
- 2026-05-20: 발표 주제 확정(에너지 절약 / 에너지 사용량 모니터링·관리 앱).
- 2026-05-20: 슬라이드 2 "관리" 제작 완료 — 키워드 20개 흩뿌림 → 중앙 응축 → "관리" 솔리드 강조.
  덱에 scroll-snap + 키보드 내비게이션 추가. 타임라인 프레임 검증 통과.
- 2026-05-20: 슬라이드 2 타이밍 보정 — "관리"가 너무 빨리 등장하던 문제 수정.
  키워드가 완전히 뭉치고 ~0.7s 머금은 뒤 "관리" 등장하도록 조정(타임라인 5.3s → 7.3s).
- 2026-05-20: 슬라이드 2 컨셉 전면 교체(사용자 요청) — "응축" 연출 폐기.
  여러 키워드가 빛나다 → 전등 꺼지듯 하나하나 소등 → 끝까지 "관리"만 남아 영화처럼 개화하는 연출로 재작성.
- 2026-05-20: 슬라이드 3 "측정"(피터 드러커 인용) 제작. 당초 슬라이드 3으로 예상하던 "솔루션·제품 소개"는
  슬라이드 4로 이동. 좌우 분할(흑백 인물 사진 | 인용구), 사진 현상 → "측정·관리·개선" 체인 점화 연출.
  `slide-3.css`·`slide-3.js`(`AiceSlide3`) 추가, `main.js` 연결.
- 2026-05-20: 슬라이드 3 수정(사용자 요청) — 사진 `peter.png`(루트) 연결, 킥커("측정 — 에너지 절약의
  출발점.") 강조 제거(균일 dim, 종료 시 인용구도 dim하지 않음), 인용구 두 문장을 각각 한 줄에 맞춤
  (폰트 축소 + `white-space:nowrap`). 메인은 인용구 두 문장.
- 2026-05-20: 슬라이드 3 킥커 문구("측정 — 에너지 절약의 출발점.") 완전 삭제(사용자 요청).
  슬라이드는 체인 점화(측정·관리·개선)로 마무리. 타임라인 ~4.55s로 단축.
- 2026-05-20: 슬라이드 4 "솔루션"(B-EMS→C-EMS) 제작 완료 — CSS 3D 와이어프레임 건물.
  1막: 건물 1채가 홀로 회전·깜빡임(B-EMS). 2막: 부감 줌아웃 → 8×4 캠퍼스 군집으로 확장,
  "B-EMS" 라벨이 "C-EMS" 헤드라인으로 변신(B→C 크로스페이드). `slide-4.css`·`slide-4.js`
  (`AiceSlide4`) 추가, `main.js`의 키보드 진행 흐름과 연결. 로컬 미리보기 검증 통과.
- 2026-05-20: 슬라이드 4 1막 수정(사용자 피드백) — 단일 건물만 떠 있어 제품 소개감이 약하던 문제를 보정.
  `건물 A-01`, `현재 전력`, `오늘 사용량`, `피크 예측`, `층별 부하` HUD를 추가해 "한 건물을 관리하는
  B-EMS 화면"처럼 보이도록 변경. 2막 C-EMS 줌아웃 구조는 유지.
- 2026-05-20: 슬라이드 4 빌딩 디자인 수정(사용자 참고 이미지 반영) — 회전 루프 제거.
  정사각형 바닥면에서 높이만 달라지는 고정 아이소메트릭 직육면체로 재설계하고, 면별 명암·창 격자·상단면을
  강화해 형태가 깨지지 않게 조정.
- 2026-05-21: 슬라이드 4 빌딩 기울기 보정 — 고정 아이소메트릭 각도를 더 정면에 가깝게 줄이고
  perspective를 멀리 둬서 빌딩이 누워 보이지 않고 똑바로 선 형태에 가깝게 조정.
- 2026-05-21: 슬라이드 4 디자인 전면 강화(사용자 요청 "디자인이 구리다 → 풍성하게").
  ▸ **빌딩 기울기 완전 해결:** `perspective` 제거(정사영) + 회전을 정적 CSS `.tilt` 래퍼로 분리.
    GSAP의 `rotationX/Y` 합성이 수직축을 비틀던 문제 제거 — 수직 모서리 `dx=0` 검증.
  ▸ **1막 관제 콘솔화:** 단순 회색 박스 → 점등 창문 격자·옥상 유닛·에너지 펄스·발밑 글로우·격자 바닥의
    업라이트 빌딩. HUD를 코너 브래킷 패널로 — 숫자 카운트업, SVG 부하 스파크라인, 굵은 층별 부하 막대(+kW),
    SVG 효율 게이지(87%). 수치는 라벨 위/큰 숫자 아래 KPI 스타일(세로 화면 오버플로 해결).
  ▸ **2막 확장 임팩트:** 그리드 바닥, 중심 코어 + 네트워크 연결선(빛 점 흐름), 집계 스트립(28개 동 등).
    줌아웃 스케일을 키워 캠퍼스가 프레임을 채우도록 조정.
  ▸ `.world` 스케일을 뷰포트 폭 비례로 — 발표 해상도 무관하게 일관. 새 의존성 없음.
  ▸ 수정: `index.html`(#slide-4) · `css/slide-4.css` · `js/slide-4.js` 전면 재작성.
- 2026-05-21: 슬라이드 4 카메라 부감 전환(사용자 피드백 "올려다보는 느낌 + 2면만 붙은 판 같다").
  원인 = `.tilt` 의 `rotateX` 가 양수라 아래에서 올려다보는 시점 → 윗면이 backface로 숨음.
  `rotateX` 를 음수(-54°)로 뒤집어 **위에서 내려다보는 부감(버드 아이)** 으로 전환 → 윗면이 드러나
  직육면체로 읽힌다. 정면/우측면/윗면 명암 분리(우측면 그늘)로 입체감 강화, 건물 비율을 더 박스답게
  조정(히어로 116×116×210), 캠퍼스를 8×4행 넓고 납작하게 재배치하고 줌·위치 재튜닝.
- 2026-05-21: 슬라이드 4 추가 보정(사용자 피드백 3건).
  ▸ **히어로 빌딩 키움** — 짜리몽땅 느낌 → 116×116×310 타워 비율로.
  ▸ **캠퍼스 흩뿌림** — 규칙적 격자 느낌 제거. 지터 ±22→±46, 셀 수보다 적게(24채) 골라 빈터 생성
    → 실제 캠퍼스처럼 불규칙 배치.
  ▸ **헤드라인 겹침 수정** — 가로 화면 Act 2에서 풀사이즈 `C-EMS` 가 부제·집계 텍스트와 겹치던 버그.
    헤드라인을 위로(`top` 5.5%), 부제·집계를 풀사이즈 헤드라인 아래로 재배치(검증: 겹침 0, 간격 14~36px).
    빌딩이 커진 만큼 1·2막 줌·위치(`ACT1/ACT2_*`)도 재튜닝.
- 2026-05-21: 슬라이드 4 Act 2(C-EMS) 풍성화(사용자 요청 "조금만 더 풍성하게").
  ▸ 캠퍼스 건물마다 점등 창문(`.clit`) 몇 개씩 — C-EMS 확립 시 도시가 하나둘 불을 켜며 살아난다.
  ▸ 코어에서 퍼지는 펄스 링 3개(`.corering`, 바닥으로 확산했다 사라짐, 반복) — 시스템 작동감.
  검증: 콘솔 에러 0건, 헤드라인↔캠퍼스 겹침 0.
- 2026-05-21: 슬라이드 4 디자인 강화 작업 커밋(`d565ebe`).
- 2026-05-21: 슬라이드 4 1막에 건물 콜아웃 추가(사용자 요청).
  빌딩 왼쪽 빈 공간에서 노드·리더 선이 끌어나오고 라벨(`단일 건물` / `건물 한 채를 통째로 관리`)이 뜬다.
  `.callout`(`.building-hud` 안 → 2막에서 함께 사라짐). 가로/세로 각각 위치 지정.
  검증: 콘솔 에러 0건, 패널·빌딩과 겹침 0(패널 6px 간격, 노드는 빌딩 모서리).
- 2026-05-21: 슬라이드 5 "예측 엔진"(머신러닝 작동 원리) 제작 완료.
  ▸ **대본 흐름:** 슬라이드 4 뒤 발표자가 실제 Campus-EMS 앱으로 전환해 메인 대시보드 데모를 보여준 뒤
    덱으로 돌아온다. 슬라이드 5는 "전력 예측"이 어떻게 만들어지는지 ML 원리만 설명(데모 화면은 덱에 미재현).
  ▸ **컨셉:** 가로 3단 수렴 파이프라인 — 입력 변수(기상 데이터 3 + 캠퍼스 특성 데이터 5) → 머신러닝
    코어 → 예측 곡선. 입력은 윤곽선 칩, 코어·출력은 솔리드 — 색이 아닌 형태 대비.
  ▸ **연출:** 칩 점등 → 연결선 드로잉 + 빛 점이 코어로 수렴 → 코어 격자 점등(학습) → 예측 곡선 +
    적정 범위 밴드 드로잉 → 킥커 `맥락을 읽는 예측`(예측 솔리드 강조). 자동 재생 단일 타임라인.
  ▸ 연결선은 `getBoundingClientRect` 기반 화면 좌표로 직선 SVG path 생성(`stroke-dashoffset` 드로잉),
    빛 점은 `attr:{cx,cy}` 보간 — GSAP 코어 AttrPlugin만, MotionPathPlugin 의존 없음.
  ▸ 재생 직전(`play()`) 좌표 재계산 → 리사이즈에도 정확. `slide-5.css`·`slide-5.js`(`AiceSlide5`) 추가,
    `index.html`(#slide-5)·`main.js` 연결. 명세는 `docs/slides.md` 슬라이드 5 항목.
  검증: 콘솔 에러 0건, 타임라인 5프레임 컴퓨티드 스타일 확인, 오버플로 0, 3단 영역 겹침 0.
- 2026-05-21: 슬라이드 5 코어·출력 전면 재설계(사용자 피드백 "좀 더 멋지게 — 진짜 가중치가
  적용되며 모델이 완성되는 느낌, 그에 따라 카운트업으로 전기 사용량이 나오는 느낌").
  ▸ **중앙:** 둥근 코어(링+격자 점) → **예측 모델 패널**. 입력별 적용 가중치(`×0.86`…)를 칩 옆에
    표시, 패널 안 **가중치 행렬(6×4)이 난수로 churn하다 한 칸씩 lock-in**, `모델 학습` 막대 0→100%,
    완성 시 패널 글로우.
  ▸ **우측:** 예측 곡선 단독 → **큰 숫자 카운트업**(`예측 전력 사용량` 0.0→142.8 kW)이 주인공,
    모델 완성 순간 차오름 + 아래 슬림 곡선 유지.
  ▸ 3단 파이프라인 골격·연결선 방식은 유지. `index.html`(#slide-5)·`css/slide-5.css`·
    `js/slide-5.js` 재작성. 타임라인 ~9s. 검증: 콘솔 에러 0건, 가중치·행렬·학습·카운트업
    전 단계 `seek(t,false)` 확인, 오버플로 0.
- 2026-05-21: 슬라이드 5 입력 그룹 묶음 보정(사용자 피드백 "기상 데이터·캠퍼스 특성 데이터
  라벨이 칩들과 너무 따로 논다").
  ▸ 각 변수 그룹을 **그룹 박스(하단 구분선 헤더 = 라벨)**로 묶어 라벨이 칩들과 한 모듈로 읽히게 함.
  ▸ 캠퍼스 특성 데이터를 5개 → 4개로 정리(`휴일` 생략, 시각 균형 3+4). `INPUT_WEIGHTS` 7개로 조정.
  ▸ 연결선 시작점을 칩별 가중치 → **입력 그룹 박스 오른쪽 모서리**로 변경(7개 선이 한 수직선에서 출발).
  검증: 콘솔 에러 0건, 박스 애니메이션·가중치 7/7·행렬·카운트업 확인, 오버플로 0.
- 2026-05-21: 슬라이드 6 "SWOT 분석" 제작(사용자 참고 이미지 기반). 2×2 매트릭스.
  ▸ 참고 이미지의 파란색은 거부된 방향이라 채택하지 않고, AGENTS.md "디자인 언어"대로 **솔리드↔윤곽선
    형태 대비**로 사분면을 가름 — 강점·기회는 솔리드, 약점·위협은 윤곽선(배지·고스트 글자·항목 마커).
  ▸ 십자 분할선 드로잉 → 네 사분면 캐스케이드 등장 → 킥커 `분석을 넘어 전략으로`(전략 강조).
  ▸ 재생 후 사분면 호버 포커스(한 칸 강조 + 나머지 디밍) — 발표자 설명용 인터랙션.
  ▸ SWOT 문구는 `QUADS` 배열에 집약. `index.html`(#slide-6)·`css/slide-6.css`·`js/slide-6.js`
    (`AiceSlide6`)·`main.js` 연결. 명세는 `docs/slides.md` 슬라이드 6 항목.
  검증: 콘솔 에러 0건, 종료 프레임 컴퓨티드 스타일 전수 확인, 오버플로 0, 호버 포커스·복원 동작 확인.
- 2026-05-21: 슬라이드 6 보정(사용자 피드백 2건).
  ▸ **고스트 글자 가시성 ↑** — 배경 S/W/O/T 글자를 더 잘 보이게(긍정 0.075→0.15,
    부정 text-stroke 0.11→0.22·1.5px).
  ▸ **호버 연출 변경** — 기존 "제자리 강조"에서 → 호버한 사분면이 **매트릭스 중앙으로 이동하며
    1.38배 확대돼 메인 카드처럼 떠오르고**(나머지 셋은 흐려짐), 호버가 빠지면 원래 자리·크기로
    복귀. 이동 거리는 `offsetLeft/Top` 기준으로 계산(transform 영향 없음·리사이즈 자동 대응).
  검증: 콘솔 에러 0건, 호버 시 확대 카드 중심=매트릭스 중심 일치(640,382)·배율 1.38 확인,
  복귀 시 transform 단위행렬·z-index 리셋 확인.
- 2026-05-21: 슬라이드 6 보정(사용자 피드백 3건).
  ▸ **매트릭스 확대** — 세로로 찌그러진 느낌 해소. 높이 `min(60vh,560px)`→`min(80vh,680px)`,
    사분면 셀 비율 2.73:1→2.05:1. eyebrow·킥커 위치를 가장자리로 당기고 패딩·폰트도 비례 확대
    (항목 하단 여백 12px→47px). 명세는 `docs/slides.md` 슬라이드 6 항목에 반영.
  ▸ **즉시 인터랙션** — 호버 활성을 인트로 종료 시점 → 슬라이드 진입 시점(`play()`)으로 변경.
    이제 페이지에 들어오면 인트로 재생 중에도 바로 호버가 동작한다.
  ▸ **확대 배율 ↑** — 호버 시 `EXPAND_SCALE` 1.38→1.62.
  검증: 콘솔 에러 0건, 셀 비율 2.05·오버플로 0·항목 여백 47px, 인트로 재생 중 호버 동작 확인
  (배율 1.62·확대 카드 중심=매트릭스 중심 640,356 일치), 복귀 정상.
- 2026-05-21: 슬라이드 6 인터랙션을 호버 → **클릭**으로 변경(사용자 피드백 "호버는 이동이 번거롭다").
  사분면 클릭 시 중앙 확대, 같은 칸 재클릭·바깥 클릭 시 복귀, 다른 칸 클릭 시 전환. `cursor:pointer`
  추가, 사분면 click 핸들러는 `stopPropagation`으로 슬라이드 레벨 닫기 핸들러와 분리.
  검증: 콘솔 에러 0건, 확대·토글 복귀·칸 전환·바깥 클릭 닫기 4종 동작 모두 확인.
- 2026-05-21: 슬라이드 2에 가로등 추가(사용자 요청 "가로등이 관리를 비추는 느낌").
  ▸ **갈고리형(구스넥) 가로등** — 화면 위쪽에서 기둥이 내려와 갈고리 팔이 휘어 "관리" 바로 위에
    등 머리가 온다. 인라인 SVG(구조·빛 빔·등 머리 글로우·발광 렌즈) + CSS 빛 웅덩이(`.lamp-pool`).
    모두 모노톤 흰빛, 새 의존성 없음.
  ▸ **연출(개화 직전 딸깍 점등):** 키워드 소등 후반에 어둠 속에서 가로등 실루엣(꺼진 등)이 조용히
    떠오름 → `alone` 시점에 "딸깍" 점등(빔·웅덩이 스냅인 + 짧은 플리커) → `bloom`에서 "관리"가
    그 불빛 안에서 개화. 배경 스포트라이트 최종값은 1→0.7로 낮춰 가로등 빛이 주 광원으로 읽히게 함.
  ▸ 수정: `index.html`(#slide-2) · `css/slide-2.css` · `js/slide-2.js`(`build()` 타임라인에
    `lampOn` 서브 타임라인 + `bloom` 라벨 추가). css/js 캐시버스트 `?v=2`→`?v=3`.
  검증: 콘솔 에러 0건. 타임라인 프레임 확인 — 키워드 단계 가로등 전부 0, 실루엣 단계 struct 1·빛 0,
  딸깍 스냅인(0.7~0.9)→플리커 dip(~0.28)→복구, 개화 시 빛 전부 1. 가로등 머리가 "관리" 위 39px,
  빔·웅덩이가 단어를 덮음. 가로(1280×720)·세로 모두 오버플로 0, 중앙 정렬 일치.
  ※ 미리보기 스크린샷 캡처가 환경 문제로 타임아웃 — 검증은 컴퓨티드 스타일·`getBoundingClientRect`로 수행.
- 2026-05-21: 슬라이드 2 가로등 보정(사용자 피드백 4건).
  ▸ **"관리" 등장 연출 개선** — "단순히 작아졌다 확 커지기만" → 어둠 속으로 살짝 가라앉았다가
    스포트라이트가 켜지면 빛 웅덩이 속으로 **떠오르며**(`y`+`scale`, `power3.out`) 또렷이 커지는
    연출. 개화 순간 점화 섬광(`coreFlare`) 후 안정.
  ▸ **노란 가로등빛** — 불빛을 흰빛 → 따뜻한 노란빛(`#ffd98e` 계열)으로. SVG 빔·등 머리 글로우·
    CSS 빛 웅덩이·"관리" 글로우·배경 스포트라이트에 적용. AGENTS.md "컬러 액센트 금지"의 의도적
    예외(사용자 명시 요청) — `docs/slides.md` 슬라이드 2 비주얼 톤에 명기.
  ▸ **등 위치 상향** — "관리" 바로 위 39px → 102px로 띄움(SVG 좌표 재배치). 빔 throw가 길어져
    스포트라이트감 강화.
  ▸ **스포트라이트 강화** — 빔·웅덩이 밝기 ↑, 빛 웅덩이가 개화에 맞춰 `scale`로 열림,
    배경 스포트라이트 최종값 0.7→0.5(주변을 더 어둑하게 해 빔이 주 광원으로 읽히게).
  ▸ 수정: `index.html`(#slide-2 SVG) · `css/slide-2.css` · `js/slide-2.js`. 캐시버스트 `v=3`→`v=4`.
  검증: 콘솔 에러 0건. 프레임 확인 — 키워드 단계 가로등 0, 실루엣 struct 1·빛 0, 딸깍 플리커
  (0.72→0.92→0.46), 개화 시 "관리" 가라앉음(ty -49)→떠오름(ty -83)·scale 0.18→1·필터
  coreFocus→coreFlare→coreHero, 빛 웅덩이 scale 0.72→1. 머리-단어 간격 102px, 가로·세로
  오버플로 0, 중앙 정렬 일치.
- 2026-05-21: 슬라이드 2 가로등 → **스포트라이트로 전면 교체**(사용자 피드백 2건).
  ▸ **가로등 기구 제거** — 기둥·갈고리 팔·등 머리(SVG 구조)를 모두 삭제. 화면 위에서 내려오는
    따뜻한 노란 **빛 원뿔(빔) + 빛 웅덩이**만 남김. "전등 없이 스포트라이트 불빛만"(사용자 요청).
    빔 점화는 SVG를 `scaleY 0→1`(상단 기준)로 — 빛줄기가 위에서 아래로 내리뻗는 연출.
  ▸ **"관리" 등장 방식 전면 변경** — "작았다 그냥 확 커지는" scale 연출 폐기. "관리"는 이제
    **처음부터 끝까지 히어로 크기 그대로**, 키워드 단계엔 보이지 않다가 스포트라이트가 켜질 때
    빛 속에서 **초점이 잡히듯 현상**된다: `blur` 16px→0(focus-pull) + 페이드인 + 살짝 떠오름(`y`)
    + 글로우 점화 섬광 → 노란빛 안정. `scale` 트윈을 일절 쓰지 않음.
  ▸ 서사 보정: "관리가 키워드 크기로 함께 빛나다 남음" → "키워드가 모두 꺼진 텅 빈 어둠에서
    스포트라이트가 답을 찾아 비춤". `docs/slides.md` 슬라이드 2 항목 갱신.
  ▸ 수정: `index.html`(#slide-2 SVG를 빔만 남김) · `css/slide-2.css`(가로등 구조 스타일 제거) ·
    `js/slide-2.js`(`build()` 재작성 — `dark`·`light`·`reveal` 라벨). 캐시버스트 `v=4`→`v=5`.
  검증: 콘솔 에러 0건. 프레임 확인 — 키워드 단계 빛 전부 0, 텅 빈 어둠, 빔 `scaleY` 0→1 점화,
  빛 웅덩이 opacity·scale 0→1, "관리" 등장 전 구간 `scale=1.000 고정`(확대 없음)·`blur` 16→0·
  `opacity` 0→1·`y` 떠오름(ty -43→-83)·필터 glowOff→glowOn→glowHero. 빔이 화면 상단(top=0)에서
  시작, "관리"·빔·웅덩이 중앙 정렬 일치, 가로(1280×720)·세로(760×1280) 오버플로 0.
- 2026-05-21: 슬라이드 6 "현장 적용"(Before → After 전환) 신규 제작 + 기존 SWOT를 슬라이드 7로
  재번호(사용자 요청 "슬라이드 5 머신러닝 뒤에 페이지 추가").
  ▸ **신규 슬라이드 6** — 학교 시설팀 직접 인터뷰로 검증한 활용 효과. 상단 인터뷰 출처 앵커
    (`현장 인터뷰` 솔리드 배지 + `영남대학교 시설팀 직접 인터뷰`) + "현재 방식(윤곽선 카드) →
    화살표 드로잉 → Campus-EMS 적용(솔리드 카드 점화)" 전환 행 2개(공용 전자기기 운영 ·
    피크 전력 관리) + 향후 방향 밴드(학생 공개 → 선의의 경쟁 → 능동적 절감). 킥커
    `예측을 넘어 운영으로`. GSAP 자동재생(~7s), 색이 아닌 형태 대비. 끝까지 남는 단어 `운영`.
  ▸ **SWOT 재번호** — `css/slide-6.css`→`slide-7.css`, `js/slide-6.js`→`slide-7.js`
    (`#slide-6`→`#slide-7`, `s6-`→`s7-`, `AiceSlide6`→`AiceSlide7`). `index.html`(SWOT 섹션
    id·링크·스크립트)·`main.js`·`docs/` 전부 반영.
  ▸ 추가: `css/slide-6.css`·`js/slide-6.js`(`AiceSlide6`). `index.html`(#slide-6, slide-5와
    slide-7 사이)·`main.js` 연결. 캐시버스트 `slide-6.* ?v=3`. 명세는 `docs/slides.md` 슬라이드 6.
  검증: 콘솔 에러 0건. 슬라이드 6 — 타임라인 종료 프레임 컴퓨티드 스타일 확인(eyebrow·인터뷰
  앵커·전환 행 2(현재/화살표/Campus-EMS)·향후 3단계·킥커 모두 opacity 1, 화살표 stroke-dashoffset
  0, `--emsglow` 0.3 안정), 콘텐츠 오버플로 0(1280×720·800×1280 모두 fitsX/Y). 슬라이드 7(SWOT)
  재번호 후 정상(사분면 4·항목 12·고스트 4·클릭 확대 인터랙션 동작 확인).
