# 슬라이드별 명세

> 각 슬라이드의 콘텐츠와 애니메이션을 정의한다. 구현의 단일 진실 소스.
> 상태 표시: 🟡 검토 중 · 🟢 확정 · ⚪ 미작성

---

## 슬라이드 1 — 인트로 (팀 Aice 등장) 🟢

**목적:** 팀 이름 Aice를 임팩트 있게 각인. "Aice = Ace + AI" 컨셉을 애니메이션으로 표현하고 팀원을 소개.

**애니메이션 시퀀스:**

1. **"Ace"** 가 가는 윤곽선(outline)으로 스르륵 떠오름 (각 글자 스태거)
2. 짧은 호흡
3. **A와 c 사이가 벌어짐** — 가운데 공간(slot)이 확장
4. 벌어진 틈으로 **"i"가 미끄러져 들어옴** (Ace 직후 곧바로 — 늦지 않게)
5. **클라이맥스** — "A"와 "i"만 안에서부터 흰색으로 **채워지고** 글로우 + 살짝 스케일. "c·e"는 윤곽선으로 남음
6. **상단 라벨("Team") · 태그라인 · 팀원 4명** 순차 공개
7. (예정) 다음 슬라이드로 전환 신호

**비주얼 톤 (확정):**
- 배경: **검정** (#0a0a0b) — 다크 시네마틱
- 글씨체: **Fraunces** 정자체 (워드마크), 본문/UI·한글은 **Pretendard**
- 색: **모노톤** (파란색·질감 그라데이션 없음)
- 강조 방식: **솔리드(채움) vs 윤곽선** 대비 — A·i 채움, c·e 윤곽선
- 모션: 튕김 없이 "스르륵" 흐르는 부드러운 이징(expo/power)

**팀원 (슬라이드 하단, 4열 깔끔 배치):**
| 역할 | 이름 | 전공 |
|---|---|---|
| 팀장 | 전승환 | 소프트웨어융합전공 |
| 팀원 | 문효은 | 환경공학전공 |
| 팀원 | 이승은 | 국어국문학과 |
| 팀원 | 조명원 | 에너지화공전공 |

**편집 가능 요소:** 타이밍, 라벨/태그라인 문구.

**상태:** 🟢 확정 · 코드 구현 완료
구현 위치: `index.html`(#slide-1 섹션) · `css/slide-1.css` · `js/slide-1.js`(`AiceSlide1`)

---

## 슬라이드 2 — 핵심 인사이트 "관리" 🟢

**목적:** 에너지 절약에는 수많은 요소가 필요하지만, 결국 끝까지 남아야 하는 것은 **관리**임을 영화처럼 각인.
서사: 여러 키워드가 모두 빛남 → 전등 꺼지듯 하나하나 소등 → 끝까지 "관리" 하나만 남아 크게 피어오름.
("불을 끈다"는 연출 자체가 에너지 절약 메타포 — 그 와중에 꺼지지 않고 남는 것이 관리.)

**콘텐츠:**

- 빛났다 꺼질 에너지 절약 키워드 20개 (솔리드 글로우 텍스트, Pretendard, 크기·밝기 차등 → 원근감):
  태양광 · 단열 · LED 조명 · 대기전력 · 고효율 가전 · 절전 모드 · 스마트 플러그 · 적정 온도 ·
  전기차 · 자연 채광 · 사용량 측정 · 피크 회피 · 인버터 · 에너지 등급 · 재생에너지 ·
  난방 효율 · 콘센트 차단 · 수요 반응 · 실시간 모니터링 · 행동 습관
- 히어로 단어 **"관리"** — Pretendard 800, 흰색 솔리드 + 글로우. 중앙에 고정, 처음엔 키워드 크기로
  축소되어 함께 빛나다가 끝에 크게 개화. 문구 없이 단어 하나.

**애니메이션 시퀀스:**

1. **점등** — 키워드들과 중앙의 "관리"가 화면 곳곳에서 하나둘 불을 켬(랜덤 스태거, 글로우 점등)
2. **호흡** — 모든 글자가 빛나는 상태로 잠시 머묾
3. **소등** — 전등 꺼지듯 키워드가 하나하나 깜빡 후 꺼짐(무작위 순서, 빨라졌다 마지막 몇 개는 감속).
   어둠이 깔리며 "관리"의 빛은 강해지고 배경 스포트라이트는 사그라듦
4. **정적** — "관리" 하나만 어둠 속에 작게 남음
5. **개화** — "관리"가 영화처럼 크게 피어오름(scale 0.18→1, 글로우 만개, 스포트라이트 다시 밝아짐)

**비주얼 톤:** 검정 배경 · 모노톤 · 흰빛 글로우 · expo/power 스무스 모션. 컬러 액센트 없음.
슬라이드 1 배경(스포트라이트·파티클·비네팅) 재사용.

**기술 메모:**

- 슬라이드 진입 시 1회 재생 — IntersectionObserver(≥55% 노출). 새 의존성 없음.
- 키워드는 JS가 5×4 그리드 + 지터로 흩뿌림(중앙은 "관리" 자리로 비움).
- 글로우는 `filter: drop-shadow`로 점등/소등. "관리"는 히어로 크기로 렌더 후 `scale`로 축소→개화(엣지 선명).
- 소등 점멸 간격은 가속 후 감속하는 곡선 — 마지막 몇 개가 천천히 꺼지며 긴장 고조.

**편집 가능 요소:** 키워드 목록(`js/slide-2.js`의 `KEYWORDS`), 타이밍.

**상태:** 🟢 확정 · 코드 구현 완료
구현 위치: `index.html`(#slide-2 섹션) · `css/slide-2.css` · `js/slide-2.js`(`AiceSlide2`)

---

## 슬라이드 3 — 자산과 에너지의 비교 (가계부 한 줄) 🟢

**목적:** 슬라이드 2에서 절약의 핵심이 *"관리"* 임을 각인했다. 슬라이드 3은 "그렇다면 관리는 어떻게
시작되는가"에 답한다. 누구나 아는 **자산(돈) 관리** — 가계부 — 에 빗대어, "이번 달 300만원" 한 줄로는
아무것도 바뀌지 않고 **언제·어디서·얼마나** 세 가지가 모두 보일 때 비로소 소비가 관리된다는 원리를
에너지에 그대로 옮긴다. 슬라이드 2가 `관리` 를 남겼듯, 슬라이드 3은 관리를 가능케 하는 세 축
**`언제·어디서·얼마나`** 를 남긴다. (이 세 축은 다음 슬라이드의 *전력 가계부* B-EMS 대시보드로 이어진다.)

**진행 방식 — 발표자 단계 제어:** 슬라이드 3은 **3단계**로 나뉘고, 단계 사이는 발표자가
**스페이스바(또는 →·PageDown)** 로 넘긴다(슬라이드 4 막 전환과 동일한 `advance()` 방식).
처음 진입 시 1단계만 보인다. 3단계까지 끝난 뒤의 다음 진행 입력은 슬라이드 4로 넘어간다.

**3단계 흐름 (발표 대본):**
1. **`이번 달 지출 300만원`** — 뭉뚱그린 한 줄.
   > "우리는 돈을 잘 모으기 위해 가계부를 씁니다. 그런데 이번 달에 300만원 썼다는 한 줄로는
   > 아무것도 바뀌지 않습니다."
2. (스페이스바) 한 줄이 세 칸으로 **확장** → `5월 20일 | 청송 리조트 | 2만원`,
   헤더 `언제 · 어디서 · 얼마나` 가 점화된다.
   > "언제 어디서 얼마나, 최소한 이 세 가지가 모두 보일 때 소비가 관리됩니다."
3. (스페이스바) 같은 세 칸 구조가 **에너지로 전환** → `5월 20일 17:10 | 중앙도서관 | 시간당 184kWh`,
   가계부 태그가 `가계부`→`에너지 가계부` 로 바뀐다.
   > "에너지도 똑같습니다. 언제, 어디서, 얼마나 쓰고 있는지 정확히 볼 수 있어야 합니다.
   > 그래야 절약이 시작됩니다."

**레이아웃 (중앙 정렬):**
- 상단 eyebrow `자산과 에너지`.
- **가계부:** 가계부 태그(`가계부`) + 본문 + 가계부 가로줄.
  본문은 **Stage 1 한 줄**(`이번 달 지출` + 큰 `300만원`)과 **Stage 2~3 세 칸 명세**가 같은 칸에 겹친다
  (`display:grid` 1/1). 세 칸 = 각각 헤더(`언제`/`어디서`/`얼마나`) + 값, 칸 사이 세로 구분선.
- 하단에 단계 표시 3점(현재 단계까지 누적 채움).

**색이 아닌 형태 대비 (AGENTS.md 디자인 언어 준수):**
- **Stage 1 한 줄:** 큰 솔리드 타이포 한 줄 — 뭉뚱그려져 무력한 총액.
- **Stage 2~3 세 칸:** 구분선으로 나뉜 세 칸 구조 + 헤더 `언제·어디서·얼마나` 솔리드 글로우 점화.

**애니메이션 시퀀스 (GSAP · 발표자 단계 제어):**
- **인트로(Stage 1):** eyebrow → 가계부 태그 → 가계부 가로줄(좌→우 드로잉) → `이번 달 지출 300만원`
  → 단계 점. (슬라이드 진입 시 자동.)
- **확장(Stage 1→2):** 한 줄이 위로 물러나고, 세 칸이 중앙에서 좌우로 펼쳐진다. 세로 구분선이
  그어지고 값이 페이드인, 헤더 `언제·어디서·얼마나` 가 위에서 드롭되며 솔리드 글로우로 점화.
- **에너지 전환(Stage 2→3):** 세 칸이 **슬롯머신 릴**처럼 값을 빠르게 돌리다 **앞 칸부터 하나씩**
  감속해 멈춘다(등속 회전 → 마지막 몇 칸 감속 착지, 도는 동안 모션 블러, 멈출 때 헤더·칸이 '딸깍'
  펄스). 각 칸이 에너지 내역에 안착하고, 가계부 태그도 `에너지 가계부` 로 플립된다.

**비주얼 톤:** 검정 배경 · 모노톤 · 흰빛 글로우 · `expo`/`power` 스무스 모션. 컬러 액센트 없음.
배경(`.bg` 스포트라이트·입자·비네팅) 슬라이드 1~2와 동일 재사용.

**기술 메모:**
- 순수 CSS + GSAP. **새 의존성 없음**, 완전 오프라인. 레이아웃은 CSS flex/grid라 리사이즈 자동 대응.
- 타임라인 3개(`tlIntro`·`tlExpand`·`tlEnergy`)를 모두 paused로 빌드. 진입 시 `tlIntro` 자동재생,
  이후 `advance()` 가 단계별로 `tlExpand`·`tlEnergy` 를 재생.
- `advance()` 는 `js/main.js` 키 핸들러(`advanceCurrent`)가 호출 — 내부 단계가 남았으면 true(덱 멈춤),
  3단계까지 끝났으면 false(덱이 다음 슬라이드로). R 키로 처음부터 재생.
- 진입 등장 트윈은 `.fromTo` 로 명시(재빌드 시 `.from` 의 잔여 상태 캡처 버그 방지).
- **슬롯 릴:** 각 칸(`.field-val`)은 `overflow:hidden` 윈도우 + 세로 `.reel` 스트립. 스트립은
  `[자산값 · 후보값들 · 에너지값]` 항목으로 JS가 생성하고, `yPercent` 로 끌어올려 마지막(에너지) 항목에
  안착한다. 칸별 항목 수·지속시간을 달리해 앞 칸부터 순차로 멈춘다. 가계부 태그만 `flipText` 사용.
- 슬라이드 추가 규칙 준수: `index.html`(#slide-3) · `css/slide-3.css`(#slide-3 스코프) ·
  `js/slide-3.js`(`AiceSlide3`) · `main.js` 연결. 클래식 `<script>`(ES 모듈 금지).

**편집 가능 요소:** 자산·에너지 내역(`js/slide-3.js`의 `ASSET`·`ENERGY` 객체 — 에너지 사용량
`184kWh` 등은 실제 값으로 교체 가능), 슬롯이 도는 후보값(`DECOYS`)·칸별 개수(`DECOY_N`),
Stage 1 문구(`index.html` `.lump`), eyebrow·태그·헤더 단어, 타이밍 상수.

**상태:** 🟢 코드 구현 완료 · 검증 통과 (사용자 시각 확인 대기)
구현 위치: `index.html`(#slide-3 섹션) · `css/slide-3.css` · `js/slide-3.js`(`AiceSlide3` {init·replay·advance})

---

## 슬라이드 4 — 솔루션 (B-EMS → C-EMS) 🟢

**목적:** 슬라이드 3의 *가계부 비유(언제·어디서·얼마나를 보여야 한다)* 에서 실제 **제품**으로 이어간다.
단일 건물 에너지 관리 시스템(B-EMS) — 슬라이드 3이 말한 그 *전력 가계부* — 을 출발점으로 삼아,
영남대학교 캠퍼스 전체를 아우르는 **C-EMS(캠퍼스 에너지 관리 시스템)** 로 확장되는 순간을 한 편의
줌아웃 카메라 무브로 보여준다.
슬라이드 3이 `언제·어디서·얼마나` 를 남겼듯 슬라이드 4는 제품명 **`C-EMS`** 를 남긴다.

**발표 대본 흐름:**

1. 화면 중앙의 단일 건물 관제 화면을 보며 `B-EMS`를 설명한다.
   - "단일 건물의 에너지를 관리하는 B-EMS"
   - "개별 건물의 전력 가계부 — 실시간 부하·층별 부하·효율"
   - "건물 하나에 적용되는 시스템을 영남대학교 캠퍼스 전체에 씌우면 어떨까?"
2. 발표자가 손을 양옆으로 펼치며 스페이스바(또는 →·PageDown)를 누른다.
3. 카메라가 하늘 위로 올라가는 듯 부감으로 줌아웃되고, 캠퍼스 건물 군집이 드러난다.
4. `B-EMS`가 `C-EMS`로 바뀌며 "캠퍼스 에너지 관리 시스템"으로 각인된다.

**서사 — 한 섹션 안의 2막 타임라인:**
- **1막 (B-EMS):** 어둠 속 중앙에 업라이트 빌딩 한 채가 떠오르고, 좌우에 관제 콘솔 패널이 켜진다.
  "한 건물의 에너지를 실시간으로 읽고 관리하는 대시보드"처럼 보여야 한다.
- **2막 (C-EMS):** 시점이 부감으로 줌아웃된다. 단일 건물은 캠퍼스 군집의 중심 코어가 되고,
  주변 건물에서 코어로 네트워크 연결선이 이어진다. `B`가 `C`로 크로스페이드되어 `C-EMS` 헤드라인으로 변신한다.

**렌더링 — 정사영(orthographic) 부감 아이소메트릭 (중요):**
- ⚠️ **빌딩은 절대 기울어 보이면 안 된다.** 원근(`perspective`)을 쓰면 높은 건물의 수직 모서리가
  위로 갈수록 모이며 "쓰러지는" 느낌이 난다. 그래서 **원근을 제거하고 정사영**으로 렌더한다.
- 회전은 정적 CSS 래퍼 `.tilt`(`rotateX(-54deg) rotateY(-36deg)` — rotateX가 바깥)가 고정으로 갖는다.
  GSAP은 `.world` 에 **스케일·세로이동만** 적용한다. → 수직 모서리가 항상 화면 수직선이 되어 안 기운다.
- ⚠️ **rotateX는 음수(-54°) — 위에서 내려다보는 부감(버드 아이).** 양수면 아래에서 올려다보게 되어
  윗면이 backface로 숨고 "두 면만 붙은 판"처럼 보인다. 음수 부감이라야 윗면이 드러나 직육면체로 읽힌다.
- 건물은 CSS 3D 직육면체 5면. 정사각형 바닥면 공유, 높이만 다르게. 정면은 밝게·우측면은 그늘지게·
  윗면은 가장 밝게 — 세 면의 명암을 분리해 입체감을 살린다.

**1막 — 단일 건물 관제 콘솔:**
- **히어로 빌딩:** DOM 창문 격자(점등/소등 셀, 일부 깜빡임), 옥상 HVAC 유닛, 바닥→옥상 에너지 펄스,
  발밑 글로우 디스크, 옅은 원근 격자 바닥.
- **좌측 패널 `REAL-TIME LOAD`:** `현재 전력`(0→128.4 kW 카운트업), `오늘 사용량`(1,842 kWh),
  `오늘 부하 곡선` SVG 스파크라인(좌→우 드로잉 + 피크 마커). 수치는 라벨 위 / 큰 숫자 아래(KPI 스타일).
- **우측 패널 `FLOOR LOAD`:** 층별 부하 막대(8F·6F·3F·B1 + kW 라벨, 0→목표 채움), 효율 게이지(SVG 반원 0→87%).
- 패널은 코너 브래킷으로 감싼 관제 화면 톤. 하단 중앙에 건물 프로필 카드(`건물 A-01`).
- **건물 콜아웃:** 빌딩 왼쪽 빈 공간(패널과 빌딩 사이)에서 노드 → 리더 선이 끌어나오고 라벨이 뜬다 —
  eyebrow `단일 건물` + `건물 한 채를 통째로 관리`. 1막 인트로 끝부분에 등장, 2막에선 HUD와 함께 사라짐.

**2막 — 캠퍼스 C-EMS:**
- 부감 줌아웃, 캠퍼스 건물 24채가 무작위 스태거로 팝인. 격자를 기준점으로 삼되 강한 지터(±46)로
  흩뿌려 실제 캠퍼스처럼 불규칙하게 배치 — 셀 수보다 적게 골라 빈터도 생긴다. 그리드 바닥 위 도시.
- 캠퍼스 건물마다 점등 창문 몇 개가 켜진다 — C-EMS가 확립될 때 도시가 하나둘 불을 켜며 살아난다.
- 중심 코어 링 + 코어에서 퍼지는 펄스 링(바닥으로 퍼졌다 사라짐, 반복) + 캠퍼스 건물 → 코어
  네트워크 연결선(선을 따라 빛 점이 흐름).
- 헤드라인 `B→C` 크로스페이드 → `C-EMS`, 부제 `캠퍼스 에너지 관리 시스템`,
  집계 스트립 `28개 동 · 4.2 MWh/일 · 실시간`.

**진행 방식 — 발표자 단계 제어:**
슬라이드 4는 두 막으로 나뉘고, 막 사이는 발표자가 **스페이스바(또는 →·PageDown)** 로 넘긴다.
1막에서 B-EMS를 설명하는 동안 화면은 단일 건물 관제 콘솔 상태로 기다린다.
2막까지 끝난 뒤의 다음 진행 입력은 다음 슬라이드로 넘어간다.

**비주얼 톤:** 검정 배경 · 모노톤 · 흰빛 글로우 · `expo`/`power` 스무스 모션. 컬러 액센트 없음.
강조는 윤곽선↔솔리드 + 명암 대비로 처리한다.

**기술 메모:**
- 순수 CSS 3D transform + 인라인 SVG(스파크라인·게이지) + GSAP. **새 의존성 없음**, 완전 오프라인.
- `.world` 스케일은 뷰포트 폭에 비례(`window.innerWidth/1280`) → 어느 발표 해상도에서도 일관.
- `js/slide-4.js`가 건물 27채(히어로 1 + 캠퍼스 24 + 히어로 옥상 유닛 2)를 생성한다.
  히어로 빌딩은 116×116×310(타워 비율), 캠퍼스 건물은 크기·높이를 다양하게 무작위 생성한다.
- 슬라이드 진입 시 1막 자동 재생: IntersectionObserver(≥55%).
- 2막 진행: `AiceSlide4.advance()`를 `js/main.js` 키 핸들러에서 호출. R 키로 처음부터 재생.
- 검증: 콘솔 에러 0건, 빌딩 4개 수직 모서리 화면 수직 확인(`dx=0`), 1·2막 패널 오버플로 0, 세로 화면 포함.

**편집 가능 요소:** 건물 수·배치(`buildCity`), 카메라 각도(`.tilt` 의 CSS `transform`),
스케일·타이밍(`ACT1/ACT2_*` 상수, `build` 타임라인), 부하 곡선 데이터(`buildSpark`의 `data`), 부제·집계 문구.

**상태:** 🟢 확정 · 코드 구현 완료
구현 위치: `index.html`(#slide-4 섹션) · `css/slide-4.css` · `js/slide-4.js`(`AiceSlide4`)

---

## 슬라이드 5 — 예측 엔진 (머신러닝 작동 원리) 🟢

**목적:** 슬라이드 4에서 C-EMS 제품을 소개하고, 발표자가 **실제 Campus-EMS 앱으로 전환해 메인
대시보드 데모**(3D 지도·전력 예측 패널·건물 클릭)를 보여준 뒤 덱으로 돌아온다. 슬라이드 5는
데모에서 본 "전력 예측"이 **어떻게** 만들어지는지 — 머신러닝 학습 원리를 시각화한다.
슬라이드 4가 `C-EMS` 를 남겼듯 슬라이드 5는 **`예측`** 을 남긴다.
> 데모 화면 자체는 실제 앱이 담당하므로 덱에 재현하지 않는다. 이 슬라이드는 ML 원리 한 장.

**발표 대본 (정리본 — 슬라이드에 깔리는 내레이션):**
> 현재 예측 결과는 머신러닝 학습을 기반으로 합니다.
> 기온·습도·일사량 같은 **기상 데이터** 와 함께, 건물의 용도·시험기간·축제·휴일 여부,
> 그리고 과거 전력 패턴 같은 **캠퍼스 특성 데이터** 를 머신러닝으로 학습시켰습니다.
> 상황에 따라 각기 다른 변수들을 매칭해 학습시켰기에, 여러 조건에서도 그 상황에 맞는 예측이 가능합니다.

**레이아웃 — 가로 3단 수렴 파이프라인 (좌→우 흐름):**
- **좌 · 입력 변수 + 가중치:** 변수 두 묶음을 각각 **그룹 박스(모듈)**로 묶는다 — 그룹 라벨이
  박스 헤더(하단 구분선)가 되고 칩들이 박스 안에 담겨, 라벨이 칩들과 한 덩어리로 읽힌다.
  각 행은 `칩 + 적용 가중치`.
  - `기상 데이터` 박스 — 칩: 기온 · 습도 · 일사량 (3개)
  - `캠퍼스 특성 데이터` 박스 — 칩: 건물 용도 · 시험기간 · 축제 · 과거 전력 패턴 (4개)
  - 칩은 **윤곽선** pill(Pretendard). 칩 오른쪽에 그 입력에 적용된 **가중치 값**(`×0.86` 등)이
    빛 점이 모델에 닿을 때 표시된다. 연결선은 그룹 박스의 오른쪽 모서리에서 출발한다.
  - 변수 목록은 정리본 기준. 대본의 `휴일`은 시각 균형(3+4)을 위해 생략 — 꼭 다 넣지 않는다.
- **중앙 · 예측 모델 패널:** 코너 브래킷 패널(슬라이드 4 관제 콘솔 톤). 헤더 `예측 모델`,
  본문에 **가중치 행렬**(6×4=24 셀), 하단에 `모델 학습` 진행 막대(0→100%).
  학습 중 행렬 셀들이 난수 가중치로 churn(빠르게 회전)하다가 한 칸씩 **lock-in**(최종값 고정·솔리드)
  되며 모델이 완성된다. 모든 입력에서 패널로 가느다란 연결선이 뻗고, 빛 점이 모델로 흘러든다.
- **우 · 예측 전력 사용량:** 라벨 `예측 전력 사용량` + **큰 숫자 카운트업**(`0.0` → `142.8 kW`,
  감속 카운트) — 모델이 100% 완성되는 순간 차오른다. 아래 슬림 예측 곡선 + 적정 범위 밴드.
- 입력 칩은 윤곽선, 모델·출력은 솔리드 — 색이 아닌 **형태 대비**.

**텍스트 (최소):**
- 상단 eyebrow: `예측 엔진`
- 하단 킥커: `맥락을 읽는 예측` — `예측`만 솔리드 강조(대본 punchline "상황에 맞는 예측"의 응축).
- 그 외 텍스트는 그룹/칩/모델/출력 라벨뿐.

**애니메이션 시퀀스 (GSAP 자동재생 타임라인, ~9s):**
1. **eyebrow** `예측 엔진` 스르륵 등장 + 빈 모델 패널이 떠오름.
2. **기상 데이터** — 그룹 박스가 떠오르고 → 칩 3개가 스태거로 점등.
3. **캠퍼스 특성 데이터** — 그룹 박스가 떠오르고 → 칩 4개 스태거 점등.
4. **수렴 + 가중치 적용** — 각 입력에서 모델로 연결선 드로잉, 빛 점이 흘러들고,
   점이 닿을 때 칩 옆에 적용 가중치(`×0.86` …)가 뜬다.
5. **학습** — 모델 가중치 행렬이 난수로 churn → `LOCK_START`부터 한 칸씩 lock-in,
   `모델 학습` 막대가 0→100%로 차오른다.
6. **모델 완성** — 24셀이 모두 잠기고 학습 100% → 패널이 글로우(테두리 점등).
7. **예측 출력** — 모델 완성에 맞춰 모델→출력 연결선이 이어지고, **예측 전력 사용량이
   `0.0`→`142.8`로 카운트업**, 슬림 예측 곡선 + 적정 범위 밴드가 드로잉된다.
8. **마무리** — 킥커 `맥락을 읽는 예측` 솔리드 개화(`예측` 글로우 강조).
- 진입 후 앰비언트: 모델 패널 글로우가 은은히 호흡, 잠긴 행렬 셀 몇 개가 미세하게 깜빡.

**비주얼 톤:** 검정 배경 · 모노톤 · 흰빛 글로우 · `expo`/`power` 스무스 모션. 컬러 액센트 없음.
배경(`.bg` 스포트라이트·입자·비네팅) 슬라이드 1~4와 동일 재사용.

**기술 메모:**
- 순수 CSS + 인라인 SVG(연결선·예측 곡선) + GSAP. **새 의존성 없음**, 완전 오프라인.
- 연결선은 화면 좌표 기반 — JS가 `getBoundingClientRect`로 가중치·모델·출력 위치를 계산해
  **직선** SVG path 생성(`stroke-dashoffset` 드로잉). 빛 점은 `attr:{cx,cy}` 보간 — GSAP
  코어 AttrPlugin만 사용, MotionPathPlugin 의존 없음.
- 가중치 행렬 churn은 churn 트윈의 `onUpdate`가 3프레임마다 미잠금 셀 텍스트를 난수로 교체,
  lock-in은 `tl.call`로 셀별 최종값 고정. 카운트업은 프록시 트윈 `onUpdate`(슬라이드 4 `addCount` 패턴).
- 슬라이드 진입 시 1회 자동재생 — IntersectionObserver(≥55% 노출). R 키로 재생.
- 폰트 로드 후(`document.fonts.ready`) 레이아웃 확정 뒤 연결선 좌표 계산. 리사이즈 시 재계산.
- 슬라이드 추가 규칙 준수: `index.html`(#slide-5) · `css/slide-5.css`(#slide-5 스코프) ·
  `js/slide-5.js`(`AiceSlide5`) · `main.js` 연결. 클래식 `<script>`(ES 모듈 금지).

**편집 가능 요소:** 입력 가중치(`js/slide-5.js`의 `INPUT_WEIGHTS`), 예측 사용량 값(`OUT_TARGET`),
행렬 셀 수(`MATRIX_N`), eyebrow·킥커 문구, 예측 곡선 데이터(`buildForecast`), 타이밍 상수.

**상태:** 🟢 코드 구현 완료 · 검증 통과 (사용자 시각 확인 대기)
구현 위치: `index.html`(#slide-5 섹션) · `css/slide-5.css` · `js/slide-5.js`(`AiceSlide5`)

---

## 슬라이드 6 — 현장 적용 (Before → After 전환) 🟢

**목적:** 슬라이드 5(예측 엔진)에서 만들어진 예측을 **실제 학교 현장**에서 어떻게 쓰는지 보여준다.
학교 시설팀 **직접 인터뷰**로 신뢰를 확보하고, "고정·단순한 현재 방식 → Campus-EMS 적용"의 두 전환
사례와 향후 방향(학생 공개)을 제시한다. 슬라이드 5가 `예측` 을 남겼듯 슬라이드 6은 **`운영`** 을 남긴다.

**발표 대본 (정리본 — 슬라이드에 깔리는 내레이션):**
> 이러한 시스템을 통해 각 학교의 시설팀은 보다 효율적으로 에너지를 관리할 수 있습니다.
> 실제로 학교 시설팀과 직접 인터뷰한 결과, 현재는 '기온 30도 이상 3일 연속'이라는 고정 기준으로
> 에어컨을 가동하고 있었고 그로 인한 민원도 많았습니다. 시스템을 활용하면 공용 전자기기 운영을
> 더 유연하고 합리적으로 관리할 수 있습니다.
> 피크 전력 관리도 현재는 건물별 순환 차단이라는 단순한 방식인데, 전력 사용량 예측 데이터를
> 활용하면 더 정교한 제어 로직을 설계할 수 있습니다.
> 향후에는 시설팀 전용 대시보드를 학생에게도 공개해, 건물별 사용량 비교로 선의의 경쟁을 유도하고
> 인센티브를 더하면 능동적인 에너지 절감을 기대할 수 있습니다.

**레이아웃 (위→아래, 중앙 정렬):**
- 상단 eyebrow `현장 적용`.
- **인터뷰 출처 앵커:** 솔리드 배지 `현장 인터뷰` + `영남대학교 시설팀 직접 인터뷰` + 흐린 한 줄
  `현장의 고정된 운영 기준을, 예측 데이터로 다시 설계합니다.` — 현장 검증의 신뢰 도장.
- **전환 행 2개:** 각 행 = `행 라벨` + [현재 카드] → [화살표] → [Campus-EMS 카드].
- **향후 방향 밴드:** 라벨 `향후 방향` + 좌→우 3단계 진행(번호 · 제목 · 부연).
- 하단 킥커 `예측을 넘어 운영으로` — `운영`만 솔리드 강조(슬라이드 8 `분석을 넘어 전략으로`와 대구).

**색이 아닌 형태 대비 (AGENTS.md 디자인 언어 준수):**
- **현재 방식:** 윤곽선 카드(빈 테두리 · 흐린 텍스트) — 경직된 옛 방식.
- **Campus-EMS 적용:** 솔리드 카드(채운 배경 · 코너 브래킷 · 점화 글로우) — 지능적 운영.

**콘텐츠 (편집 가능 — `js/slide-6.js`의 `ROWS`·`FUTURE` 배열):**
- 전환 행 1 `공용 전자기기 운영` — 현재 `기온 30°C 이상 · 3일 연속`(고정된 가동 기준 — 잦은 민원)
  → Campus-EMS `상황을 읽는 유연한 운영`(합리적인 공용기기 제어).
- 전환 행 2 `피크 전력 관리` — 현재 `건물별 순환 전원 차단`(일괄적이고 단순한 차단 방식)
  → Campus-EMS `예측 기반 정교한 제어`(전력 사용량 예측으로 제어 로직 설계).
- 향후 방향 — `① 학생에게 대시보드 공개` · `② 건물별 사용량 비교` · `③ 인센티브로 능동적 절감`.

**애니메이션 시퀀스 (GSAP 자동재생 타임라인, ~7s):**
1. eyebrow `현장 적용` 스르륵 등장.
2. 인터뷰 출처 앵커 — 배지·출처 한 줄이 떠오르고 흐린 서브 라인이 페이드인.
3. 전환 행(각 행 순서대로): 행 라벨 → 현재 카드(윤곽선) 떠오름 → 화살표가 좌→우로 드로잉
   → Campus-EMS 카드가 솔리드로 **점화**(글로우 섬광 후 은은히 안정).
4. 향후 방향 밴드 — 밴드가 떠오르고 3단계가 좌→우 캐스케이드(▸ 구분자 팝인).
5. 마무리 킥커 `예측을 넘어 운영으로` — `운영` 솔리드 글로우 강조.
6. 진입 후 앰비언트: Campus-EMS 카드 글로우가 은은히 호흡.

**비주얼 톤:** 검정 배경 · 모노톤 · 흰빛 글로우 · `expo`/`power` 스무스 모션. 컬러 액센트 없음.
배경(`.bg` 스포트라이트·입자·비네팅) 슬라이드 1~5와 동일 재사용.

**기술 메모:**
- 순수 CSS Flex + 인라인 SVG(전환 화살표) + GSAP. **새 의존성 없음**, 완전 오프라인.
  CSS 기반 레이아웃이라 리사이즈 자동 대응(세로 화면은 카드·단계를 세로 적층).
- 전환 행·향후 단계는 `ROWS`·`FUTURE` 배열에 두고 JS가 DOM을 생성 — 문구 수정이 한곳에서
  가능(슬라이드 2 `KEYWORDS`·8 `QUADS` 패턴).
- 화살표는 인라인 SVG path를 `getTotalLength` 기반 `stroke-dashoffset` 으로 드로잉.
  Campus-EMS 카드 점화는 CSS 변수 `--emsglow` 를 GSAP로 트윈(슬라이드 5 `--mglow` 패턴).
- 슬라이드 진입 시 1회 자동재생 — IntersectionObserver(≥55% 노출). R 키로 재생.
- 슬라이드 추가 규칙 준수: `index.html`(#slide-6) · `css/slide-6.css`(#slide-6 스코프) ·
  `js/slide-6.js`(`AiceSlide6`) · `main.js` 연결. 클래식 `<script>`(ES 모듈 금지).

**편집 가능 요소:** 전환 행 문구(`ROWS`), 향후 단계(`FUTURE`), eyebrow·킥커·인터뷰 출처 문구,
등장 타이밍(`ROW_AT`·`FUTURE_AT`·`KICK_AT`).

**상태:** 🟢 코드 구현 완료 (사용자 시각 확인 대기)
구현 위치: `index.html`(#slide-6 섹션) · `css/slide-6.css` · `js/slide-6.js`(`AiceSlide6`)

---

## 슬라이드 7 — "측정" (피터 드러커 인용) 🟢

**목적:** 슬라이드 4~6에서 제품(C-EMS)·예측·현장 운영을 모두 펼쳐 보인 뒤, 다시 **본질**로 돌아온다.
경영학의 아버지 피터 드러커의 명언으로 **측정 → 관리 → 개선**의 논리 사슬을 각인해,
"지금까지 보여준 모든 것은 결국 *측정* 에서 출발한다"를 짚고 — 이어지는 SWOT(전략적 자가진단)의 디딤돌로 삼는다.
슬라이드 6까지 제품·예측·운영을 펼쳤듯, 슬라이드 7은 그 모든 것의 토대인 `측정` 을 남긴다.
(SWOT 분석 또한 *측정·진단* 의 한 형태 — `측정 → 분석 → 전략` 으로 자연스럽게 잇는다.)

**레이아웃 — 좌우 분할:**
- **좌측(약 42%):** 흑백 인물 사진. `object-fit: cover`. 안쪽(우측) 가장자리는 검정 그라데이션으로
  페이드 → 분할선이 배경에 녹아듦. 사방을 옅게 비네팅해 다크 톤에 통합.
- **우측(약 58%):** 출처 라벨 · 히어로 인용구 · 킥커.
- 배경(`.bg` 스포트라이트·입자·비네팅)은 슬라이드 1~6과 동일하게 재사용(스포트라이트는 사진 쪽으로 치우침).

**콘텐츠 · 문구 (편집 가능):**
- **출처 라벨:** `PETER F. DRUCKER` / `경영학의 아버지` — 슬라이드 1 `Team` 라벨 톤. 아래 얇은 룰 라인.
- **히어로 인용구** (Pretendard, 본문은 흐리게 dim) — 슬라이드의 **메인**. 각 문장은 한 줄에 들어간다:
  > 측정할 수 없으면 관리할 수 없고,
  > 관리할 수 없으면 개선할 수 없다.
- **체인 키워드:** `측정 · 관리 · 개선` 만 솔리드 흰색 + 글로우 — "색이 아닌 형태 대비" 강조.
- 대본의 마무리 문장("…에너지 절약의 출발점입니다")은 발표자가 말로 전달하고, 슬라이드에는 별도 문구를 두지 않는다.

**애니메이션 시퀀스 (GSAP 타임라인, 약 6s):**

1. **현상** — 어둠 속에서 스포트라이트가 번지며 사진이 낮은 명암 → 정상으로 서서히 *인화*
   (opacity·brightness 상승, scale 1.06→1). 암실 인화 느낌, `expo/power`.
2. **라벨** — 우측 상단 출처 라벨 페이드인 + 룰 라인이 좌→우로 그어짐.
3. **인용구 본문** — 큰 따옴표와 줄이 단위로 스르륵 떠오름(line reveal). 키워드는 아직 흐림.
4. **체인 점화 (피날레)** — `측정` → `관리` → `개선` 순서로 한 단어씩 솔리드로 채워지며 글로우 점화
   (미세 scale 펄스). `관리` 점화는 슬라이드 2 솔리드 강조의 메아리(두 인스턴스 동시 점화).
   세 단어가 모두 켜진 채 슬라이드가 마무리된다.

**비주얼 톤:** 검정 배경 · 모노톤(흑백 사진+흰 텍스트) · 흰빛 글로우 · expo/power 스무스 모션.
컬러 액센트 없음. 슬라이드 1~6 배경 재사용.

**기술 메모:**

- 슬라이드 진입 시 1회 재생 — IntersectionObserver(≥55% 노출). R 키로 재생. 새 의존성 없음.
- 사진은 프로젝트 루트의 `peter.png`(로컬, 완전 오프라인). `<img>` 로드 실패 시 깨진 아이콘을 숨겨
  좌측 패널이 어둠으로 우아하게 대체되도록 처리.
- 사진 "현상"은 CSS `filter`(grayscale·brightness·contrast) + opacity·scale 을 GSAP로 트윈.
- 체인 키워드는 인라인 `<em class="kw">`(`display:inline-block`), 점화는 color + `drop-shadow` 글로우.

**편집 가능 요소:** 인용구 줄바꿈, 출처 라벨, 타이밍, 사진 파일.

**상태:** 🟢 확정 · 코드 구현 완료
구현 위치: `index.html`(#slide-7 섹션) · `css/slide-7.css` · `js/slide-7.js`(`AiceSlide7`)
사진 에셋: `peter.png` (프로젝트 루트 · 사용자 제공)

---

## 슬라이드 8 — SWOT 분석 🟢

**목적:** 슬라이드 7까지 제품·기술·활용과 그 토대인 *측정* 을 짚은 뒤, Campus-EMS를 **냉정하게 자가 진단**한다.
강점·약점·기회·위협을 한 화면에서 보여 주어 "제품을 안다 → 다음 전략이 보인다"로 잇는 분석 슬라이드.
슬라이드 2가 `관리`, 3이 `언제·어디서·얼마나`, 4가 `C-EMS`, 5가 `예측`, 6이 `운영`, 7이 `측정` 을
남겼듯 슬라이드 8은 **`전략`** 을 남긴다.

**레이아웃 — 2×2 매트릭스:**
- 상단 eyebrow `SWOT 분석`. 중앙에 외곽 프레임 + 십자 분할선으로 나뉜 네 사분면. 하단 킥커.
- 사분면 배치(참고 이미지와 동일): 좌상 `S 강점` · 우상 `W 약점` · 좌하 `O 기회` · 우하 `T 위협`.
- 각 사분면: 큰 고스트 글자(S/W/O/T, Fraunces) + 배지·라벨(한글/영문) + 항목 3개(굵은 리드 + 흐린 부연).

**색이 아닌 형태 대비 (AGENTS.md 디자인 언어 준수):**
- 참고 이미지의 파란색은 쓰지 않는다(거부된 방향). 대신 **솔리드 ↔ 윤곽선**으로 사분면을 가른다.
- **강점·기회(긍정):** 솔리드 배지(채운 사각형) + 솔리드 고스트 글자 + 채운 항목 마커.
- **약점·위협(부정):** 윤곽선 배지(빈 사각형) + 윤곽선(text-stroke) 고스트 글자 + 빈 항목 마커.

**콘텐츠 (편집 가능 — `js/slide-8.js`의 `QUADS` 배열. 글은 의도적으로 압축):**
- **S 강점:** 선제적 운영 · 데이터 기반 의사결정 · 운영 효율 개선
- **W 약점:** 예측 모델 오차 · 제어 연동 한계 · UI/UX 직관성
- **O 기회:** 정책 연계 · 대외 경쟁력 · 자산화·확장성
- **T 위협:** 차별성 입증 · 규제·행정 장벽 · 지속가능성 확보

**애니메이션 시퀀스 (GSAP 자동재생 타임라인, ~6s):**
1. eyebrow `SWOT 분석` 스르륵 등장.
2. 외곽 프레임 페이드인 → **십자 분할선이 중앙에서 좌우·상하로 그어짐**(`expo.out`) → 중심 노드 팝.
3. 네 사분면이 읽기 순서(S→W→O→T)로 등장 — 카드가 떠오르면 고스트 글자·배지·라벨·항목 3개가 캐스케이드.
4. 마무리 킥커 `분석을 넘어 전략으로` — `전략` 솔리드 강조.
5. 진입 후 앰비언트: 중심 노드 글로우가 은은히 호흡.

**인터랙티브 — 사분면 클릭 확대 (슬라이드 진입 즉시 활성):**
- 사분면을 **클릭**하면 그 사분면이 **매트릭스 중앙으로 이동하며 확대**(`EXPAND_SCALE`, 기본 1.62배)
  돼 메인 카드처럼 떠오르고, 나머지 셋은 제자리에서 물러나며 흐려진다.
- 같은 칸을 다시 클릭하거나 바깥 빈 영역을 클릭하면 원래 자리·크기로 복귀. 다른 칸을 클릭하면 그 칸으로 전환.
- 클릭 인터랙션은 인트로 재생 중에도 동작한다 — 발표자가 사분면을 하나씩 짚으며 설명할 수 있다.

**비주얼 톤:** 검정 배경 · 모노톤 · 흰빛 글로우 · `expo`/`power` 스무스 모션. 컬러 액센트 없음.
배경(`.bg` 스포트라이트·입자·비네팅) 슬라이드 1~7과 동일 재사용.

**기술 메모:**
- 순수 CSS Grid(2×2) + GSAP. **새 의존성 없음**, 완전 오프라인. 레이아웃이 CSS 기반이라 리사이즈 자동 대응.
- SWOT 내용은 `QUADS` 배열에 두고 JS가 사분면 DOM을 생성 — 문구 수정이 한곳에서 가능(슬라이드 2 `KEYWORDS` 패턴).
- 슬라이드 진입 시 1회 자동재생 — IntersectionObserver(≥55% 노출). R 키로 재생.
- 슬라이드 추가 규칙 준수: `index.html`(#slide-8) · `css/slide-8.css`(#slide-8 스코프) ·
  `js/slide-8.js`(`AiceSlide8`) · `main.js` 연결. 클래식 `<script>`(ES 모듈 금지).

**편집 가능 요소:** SWOT 문구(`QUADS`), eyebrow·킥커 문구, 사분면 등장 타이밍(`QUAD_AT`),
호버 확대 배율(`EXPAND_SCALE`).

**상태:** 🟢 코드 구현 완료 (사용자 시각 확인 대기)
구현 위치: `index.html`(#slide-8 섹션) · `css/slide-8.css` · `js/slide-8.js`(`AiceSlide8`)

---

## 슬라이드 9 이후 ⚪

프로젝트: **에너지 사용량 모니터링·관리 앱(Campus-EMS).**
발표 흐름: 슬라이드 1 인트로 → 2 핵심 인사이트("관리") → 3 자산과 에너지의 비교(가계부 비유)
→ 4 솔루션(B-EMS→C-EMS) → [실제 앱 데모] → 5 예측 엔진(ML 원리) → 6 현장 적용(활용·인터뷰)
→ 7 측정(드러커 인용) → 8 SWOT 분석 → **마무리** (미정).
